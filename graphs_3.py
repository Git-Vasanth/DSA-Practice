# Detect Cycle in an Undirected Graph

# 📝 Problem Statement

"You are given a list of edges that form an undirected graph. Your task is to detect if the graph contains a cycle."


def has_cycle(edges: list[tuple[str, str]]) -> bool:

    pass


edges = [
    ("A", "B"),
    ("B", "C"),
    ("C", "A")
]
